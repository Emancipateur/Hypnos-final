<form method="post" action="{{ path('app_suites_delete', {'id': suite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suite.id) }}">
    <button class="btn">Supprimer la suite</button>
</form>
